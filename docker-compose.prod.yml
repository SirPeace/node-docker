version: "3"
volumes:
  production-database:
services: 
  app: 
    build: 
      context: .
      args: 
        NODE_ENV: production
    ports:
      - 3000:4000
    env_file: ./.env.prod
    command: npm run start

  database:
    volumes:
      - production-database:/data/db # Named volume