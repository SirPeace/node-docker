version: "3"
services: 
  app:
    build: 
      context: .
      args: 
        NODE_ENV: development
    ports:
      - 3000:3000
    volumes:
      - ./:/app # Bind-mount volume
      - /app/node_modules # Anonymous volume
    env_file: ./.env.dev
    command: npm run dev
    environment: 
      - SESSION_SECRET="don't get cocky"